/*"use strict";

const html = require("html");
import * as jade from "jade";
import Changelog from "../model/changelog";
import APIDefinition from "../model/apiDefinition";
import ChangeDescription from "../model/changeDescription";

export default class DefinitionFileGenerator {

  public static generate(def: APIDefinition, changelog: Changelog): string {

    return "";
  }
}
*/
